# Core runtime (engine + API). Enough to run the app on the sample dataset.
nltk>=3.8
fastapi>=0.110
uvicorn[standard]>=0.27

# ---------------------------------------------------------------------------
# Optional add-ons (kept in separate files so the core install stays light):
#
#   pip install -r requirements-bert.txt   # semantic (BERT) query expansion (heavy: PyTorch)
#   pip install -r requirements-dev.txt    # pytest, for running the test suite
#
# The Kaggle CLI (`pip install kaggle`) is only needed as a fallback for
# scripts/download_data.py; the default download uses a public mirror and needs
# no extra package or account.
# ---------------------------------------------------------------------------
